# Supabase 庫存搬遷計畫

狀態：**已完成正式切換；本文件保留為歷史規劃與回歸清單**  
建立日期：2026-07-17  
狀態更新：2026-09-07

目前真實狀態以 `ERP_CURRENT_STATE.md` 為單一入口。庫存、BOM、異動紀錄、領料與入料/QC 已是 Supabase 主資料，Notion 為人員查閱鏡像或明確唯讀備援；不得再依本文件早期階段文字把正式流程切回 Notion。

## 目標

先將庫存領域逐步搬遷至 Supabase，但不能只修改庫存分頁。所有會影響庫存的 ERP 流程都必須透過同一套庫存服務處理，避免 Supabase、Notion 與網頁出現不同數量。

## 目標架構

- Supabase：料號、BOM、庫存餘額與庫存異動的唯一正式來源。
- 線上 ERP：庫存查詢及所有增減操作只對 Supabase 執行。
- Notion：保留人工查看與管理介面；人工修改轉成變更申請，再由系統寫入 Supabase。
- Supabase 成功異動後，使用同步佇列更新 Notion 鏡像。
- Notion 同步失敗只標示待同步及重試，不得重新執行庫存異動。

## 必須納入的流程

1. 手動建立、編輯、封存、刪除及合併料號。
2. 一般國內／國外訂單領料與無 BOM 外購成品扣庫。
3. 半成品組裝扣除直接零件，完成後增加半成品。
4. 蝦皮生產訂單扣除直接 BOM，完成後增加 `S-` 成品。
5. C 端出貨直接扣除 `S-` 庫存；取消時依選擇回庫或報銷。
6. 入料建立、品管通過入庫、退回與重新提交。
7. 臨時領料、補料、手動調整及盤點。
8. PI、Excel 匯入時自動建立新料號。
9. 重複料號合併、庫存轉移及 BOM 關聯修復。

## 固定規則

- 料號必須精確比對；前導零、連字號、加號與其他符號不可自動改寫。
- 成品只扣 BOM 直接掛載的半成品與零件，不遞迴拆解半成品。
- 半成品組裝單扣除其直接零件，完成後增加半成品庫存。
- 蝦皮生產單扣除直接 BOM，完成後增加 `S-` 成品。
- C 端出貨只扣 `S-` 成品本身，不展開 BOM。
- 每次庫存異動必須有唯一冪等鍵，重複按鈕或重試不得重複增減。
- 庫存異動須使用資料庫交易與列鎖，避免多人操作造成負數或覆寫。

## 執行階段

### Phase 1：修復測試架構

- 修復現有 Supabase SQL 草稿中的中文亂碼及損壞字串。
- 加入同步佇列、Notion 變更申請、衝突紀錄、重試及權限政策。
- 僅建立測試環境，不修改正式 ERP、Notion 或正式庫存。

### Phase 2：唯讀搬遷演練

- 從 Notion 匯入料號、別名、BOM 與期初庫存。
- 產出 dry-run 報告，比對筆數、料號、類型、庫存及 BOM 數量。
- 發現差異時停止，不自動猜測或合併。

### Phase 3：影子驗證

- 正式 ERP 暫時仍以 Notion 執行庫存異動。
- 同時在 Supabase 建立不影響正式餘額的預計異動紀錄。
- 比對 Notion 實際結果與 Supabase 預計結果，禁止雙重扣料。

### Phase 4：統一庫存服務

- 建立 `InventoryService`，集中處理查詢、建檔、入庫、扣庫、退庫、調整、BOM 領料與合併。
- 移除各流程直接修改 Notion `目前庫存` 的行為。
- 保留後端切換開關：`notion`、`shadow`、`supabase`。

### Phase 5：完整迴歸測試

- 一般成品訂單及無 BOM 外購成品。
- 半成品組裝、品管完成及入庫。
- 蝦皮生產、完成及 `S-` 入庫。
- C 端出貨、取消回庫與報銷。
- 入料、品管通過、退回、重新提交。
- 補料、手動調整、建立新料號及重複料號合併。
- 缺料、多人同時操作、重複點擊、離線重試及 Notion API 失敗。

### Phase 6：正式切換

- 影子模式至少連續 3 個工作日或 100 次庫存異動零差異，以較晚達成者為準。
- 切換前完整備份 Notion 與 Supabase。
- 短暫停止 Notion 直接修改庫存，再將 ERP 切換至 Supabase。
- 切換後 Notion 僅作鏡像與變更申請入口。

## 驗收條件

- 所有料號、類型、前導零與符號完全一致。
- 所有有效 BOM 的母件、子件及數量完全一致。
- 每個料號／倉別的期初庫存一致。
- ERP 內不存在繞過庫存服務的直接 Notion 庫存寫入。
- 所有流程測試通過，重複操作不會重複增減。
- Notion 同步失敗可重試，且不影響 Supabase 正式庫存。
- 備份、切換開關及回復程序已實際演練。

## 歷史暫停範圍

此段僅記錄 2026-07-17 規劃當時的限制，現在不再是有效阻擋。後續變更仍須依 `ERP_SYSTEM_CONTRACT.md` 執行 dry-run、測試、部署與 production readback。
